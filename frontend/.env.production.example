# Static assets are served from https://eaea499.cn/esp32/
VITE_BASE_PATH=/esp32/

# Nginx proxies this same-origin prefix to the Spring Boot backend.
VITE_API_BASE_URL=/esp32-api

# Camera URLs remain local until a separate secure video access path is deployed.
# VITE_CAMERA_BASE_URL=http://ESP32_CAM_IP
